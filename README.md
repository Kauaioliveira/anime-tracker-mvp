# Anime Tracker (MVP)

React + TypeScript + Vite app that lists anime from the [Jikan API](https://docs.api.jikan.moe/) (search and top titles).

## Scripts

- `npm run dev` — start dev server
- `npm run build` — typecheck + production build
- `npm run lint` — ESLint
